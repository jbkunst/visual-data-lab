options(
  bslib.color_contrast_warnings = FALSE,
  shiny.minified = TRUE,
  shiny.autoreload = FALSE,
  shiny.autoreload.legacy_warning = FALSE,
  sass.cache = TRUE,
  bslib.precompiled = TRUE
)

conflictRules("bslib", mask.ok = "page")
